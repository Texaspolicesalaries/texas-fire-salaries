node_modules/
.DS_Store
.wrangler/
.firebase/
*.log

# Service-account keys must NEVER be committed (we don't need one, but just in case).
service-account.json
serviceAccountKey.json
*-firebase-adminsdk-*.json

# Generated by the build (`npm run refresh` / `npm run build`) — regenerated in the
# Cloudflare Pages build, so kept out of git to avoid noisy diffs on every data change.
/departments/
/counties/
/regions/
/rankings/
/sitemap.xml
